<section class="container">
  <h3 class="portfolio-name">{{portfolio.portfolio_name}}</h3>
  <section class="flex-row justify-center">
    {{#each portfolio.artworks as |artwork|}}
    {{> artwork-details}}
    {{/each}}
  </section>
</section>