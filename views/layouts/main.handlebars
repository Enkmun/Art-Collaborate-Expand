<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ACE</title>
</head>
<body>
      <h1>Welcome to A.C.E</h1>
  </header>
  <nav>
    <a href="/">Home</a>
    {{#if loggedIn}}
    <button id="logout" class="btn-no-style">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}

  </nav>
  <main>
    <aside>
      <h2>Enjoy the A.C.E!</h2>
    </aside>
    <div class="gallery">
        {{#each products}}
            <div class="product">
                <h2>{{title}}</h2>
                <p>{{description}}</p>
                <p>Price: ${{price}}</p>
                <img src="{{imageUrl}}" alt="Product Image">
            </div>
            {{/each}}
      {{{body}}}
    </div>
  </main>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html>